class Fondo{

  // -----------------------------------------------------
  // CAMPOS (variables) #propiedes
  /*
  PImage
  // Posición y tamaño
  int x, y, tam;
  
  */
  // -----------------------------------------------------
  // CONSTRUCTOR > metodo/funcion inicial (el "setup" del objeto)
    // inicializar posicion y tamaño mediante parámetros
//loadImage(fondo);

  // -----------------------------------------------------
  // METODOS (funciones) #acciones
  // dibujar()
  // dibujar fondo
  //image(fondo, height, widht);
}
